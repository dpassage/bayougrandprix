<h1>Listing season_entries</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @season_entries.each do |season_entry| %>
  <tr>
    <td><%= link_to 'Show', season_season_entry_path(@season, season_entry) %></td>
    <td><%= link_to 'Edit', edit_season_season_entry_path(@season, season_entry) %></td>
    <td><%= link_to 'Destroy', season_season_entry_path(@season, season_entry), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Season entry', new_season_season_entry_path(@season) %>
